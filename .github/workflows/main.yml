name: SBOM Generation & Analysis

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  schedule:
    # Gerar SBOM semanalmente
    - cron: '0 3 * * 1'
  workflow_dispatch:

permissions:
  contents: write
  security-events: write
  actions: read

jobs:
  generate-sbom-python:
    name: Generate Python SBOM
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install Syft
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh | sh -s -- -b $HOME/bin
          echo "$HOME/bin" >> $GITHUB_PATH


      
      - name: Generate SBOM com Syft (SPDX e CycloneDX)
        run: |
          syft packages dir:. -o spdx-json=sbom-python-spdx.json
          syft packages dir:. -o cyclonedx-json=sbom-python-cyclonedx.json
      
      - name: Generate SBOM com CycloneDX Python
        run: |
          pip install cyclonedx-bom
          cyclonedx-py requirements -r -i requirements.txt -o sbom-python-cdx.json
      
      - name: Upload SBOM artifacts
        uses: actions/upload-artifact@v4
        with:
          name: python-sboms
          path: |
            sbom-python-*.json
          retention-days: 90
      
      - name: Commit SBOM to repository
        run: |
          mkdir -p sbom
          mv sbom-python-*.json sbom/
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add sbom/
          git diff --quiet && git diff --staged --quiet || git commit -m "chore: update Python SBOM [skip ci]"
          git push || echo "No changes to push"
        continue-on-error: true

  analyze-licenses:
    name: License Analysis
    needs: [generate-sbom-python]
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4
      
      - name: Download SBOM artifacts
        uses: actions/download-artifact@v4
        with:
          name: python-sboms
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install pip-licenses e dependências
        run: |
         pip install -r ./requirements.txt
          pip install pip-licenses
      
      - name: Generate License Report
        run: |
          pip-licenses --format=json --output-file=licenses.json
          pip-licenses --format=markdown --output-file=licenses.md
          pip-licenses --format=csv --output-file=licenses.csv
      
      - name: Check for GPL licenses (exemplo de policy)
        run: |
          echo "🔍 Verificando licenças GPL..."
          if pip-licenses | grep -i "gpl"; then
            echo "⚠️  ATENÇÃO: Dependências com licença GPL detectadas!"
            echo "Revise a compatibilidade com a licença do projeto"
          else
            echo "✅ Nenhuma licença GPL detectada"
          fi
        continue-on-error: true
      
      - name: Upload License Report
        uses: actions/upload-artifact@v4
        with:
          name: license-reports
          path: |
            licenses.*
          retention-days: 90
      
      - name: Create License Summary
        run: |
          echo "# 📄 Relatório de Licenças" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          pip-licenses --format=markdown >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY

  vulnerability-scan-sbom:
    name: Scan SBOM for Vulnerabilities
    needs: [generate-sbom-python]
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4
      
      - name: Download SBOM
        uses: actions/download-artifact@v4
        with:
          name: python-sboms
      
      - name: Install Grype
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh | sh -s -- -b /usr/local/bin
      
      - name: Scan SBOM com Grype
        run: |
          grype sbom:sbom-python-spdx.json -o json --file grype-sbom-results.json
          grype sbom:sbom-python-spdx.json -o table > grype-sbom-results.txt
        continue-on-error: true
      
      - name: Upload Vulnerability Report
        uses: actions/upload-artifact@v4
        with:
          name: vulnerability-reports
          path: |
            grype-sbom-results.json
            grype-sbom-results.txt
          retention-days: 90

  dependency-tree:
    name: Generate Dependency Tree
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install pipdeptree e dependências
        run: |
          pip install -r ./requirements.txt
          pip install pipdeptree
      
      - name: Generate Dependency Tree
        run: |
          pipdeptree --json > dependency-tree.json
          # pipdeptree --graph-output png > dependency-tree.png # Requer graphviz, que pode não estar instalado
          pipdeptree > dependency-tree.txt
      
      - name: Upload Dependency Tree
        uses: actions/upload-artifact@v4
        with:
          name: dependency-tree
          path: |
            dependency-tree.*
          retention-days: 30

  sbom-summary:
    name: SBOM Summary Report
    needs: [generate-sbom-python, analyze-licenses, vulnerability-scan-sbom]
    runs-on: ubuntu-latest
    if: always()
    
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
      
      - name: Create comprehensive summary
        run: |
          echo "# 📦 Software Bill of Materials (SBOM)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📊 Status dos Jobs" >> $GITHUB_STEP_SUMMARY
          echo "- Python SBOM: ${{ needs.generate-sbom-python.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- License Analysis: ${{ needs.analyze-licenses.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- Vulnerability Scan: ${{ needs.vulnerability-scan-sbom.result }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📋 Formatos Gerados" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ SPDX JSON" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ CycloneDX JSON" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ License Report (JSON, MD, CSV)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Dependency Tree" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 🔗 Artefatos" >> $GITHUB_STEP_SUMMARY
          echo "Todos os SBOMs e relatórios estão disponíveis como artifacts desta execução." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📅 Gerado em: $(date)" >> $GITHUB_STEP_SUMMARY
